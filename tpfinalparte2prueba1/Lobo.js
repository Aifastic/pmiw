class Lobo{
  constructor() {
    this.posX = width/2;
    this.posY = height/2;
    this.tiempo = 60;
    this.color = color(129,98,77);
  }
  
  dibujar() {
    if (escenario == 2 && tiempo =% 30);
    fill(this.color);
    ellipse(this.posX, this.posY, 50, 50);
  }
}
